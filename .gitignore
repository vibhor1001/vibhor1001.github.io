.DS_Store
.claude/
*.pdf
*.pdf:Zone.Identifier
*:Zone.Identifier
Problem/

# Internal design / spec docs — not for public site moving forward.
# (Already-tracked files in /docs/ remain tracked.)
/docs/

# Review-pack reference files shared internally — not for the repo.
/unused/
